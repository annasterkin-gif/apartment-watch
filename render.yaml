services:
  - type: web
    name: apartment-watch-api
    runtime: node
    buildCommand: npm install && npx playwright install chromium --with-deps
    startCommand: xvfb-run -a node server.js
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
